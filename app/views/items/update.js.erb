<% if @item.errors.any? %>
  
<% else %>
  $("tr.items").remove();
  $("table:last-child").append("<%= escape_javascript(render('items/items'))%>");
  $('.modal, .modal-backdrop').remove();
<% end %>
