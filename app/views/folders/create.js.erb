<% if @folder.errors.any? %>
  $("#error").remove();
  $('.modal-body').prepend("<%=escape_javascript(render :partial => 'layouts/error_messages', :object => @folder)%>");
<% else %>
  $(".folders").replaceWith("<%= escape_javascript(render('folders/folders'))%>");
<% end %>